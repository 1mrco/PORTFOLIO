<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %> | <%= siteTitle %></title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <div class="page-root">
      <header class="site-header">
        <nav class="nav">
          <a href="/" class="nav-link">Home</a>
          <a href="/projects" class="nav-link">Projects</a>
          <a href="/certificates" class="nav-link nav-link-active">Certificates</a>
          <a href="/games" class="nav-link">Games</a>
        </nav>
        <button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme">
          <span class="theme-text"></span>
        </button>
      </header>

      <main class="main-content">
        <section class="section-header">
          <h1>Certificates</h1>
          <p>Placeholder certificates. Real certificates can be added or removed easily.</p>
        </section>

        <section class="card-grid">
          <% if (certificates.length === 0) { %>
            <p style="color: var(--muted); text-align: center; grid-column: 1 / -1;">
              No certificates yet. Add certificates by editing <code>data/certificates.json</code>
            </p>
          <% } else { %>
            <% certificates.forEach((cert) => { %>
              <article class="card certificate-card interactive-card">
                <div class="card-body">
                  <h2 class="card-title">
                    <% if (cert.link) { %>
                      <a href="<%= cert.link %>" target="_blank" rel="noreferrer" style="color: inherit; text-decoration: none;">
                        <%= cert.title %>
                      </a>
                    <% } else { %>
                      <%= cert.title %>
                    <% } %>
                  </h2>
                  <p class="card-text"><%= cert.description %></p>
                  <% if (cert.issuer || cert.date) { %>
                    <div style="margin-top: 0.75rem; font-size: 0.85rem; color: var(--muted);">
                      <% if (cert.issuer) { %>
                        <div><strong>Issuer:</strong> <%= cert.issuer %></div>
                      <% } %>
                      <% if (cert.date) { %>
                        <div><strong>Date:</strong> <%= cert.date %></div>
                      <% } %>
                    </div>
                  <% } %>
                </div>
              </article>
            <% }); %>
          <% } %>
        </section>
      </main>

      <footer class="site-footer">
        <div class="footer-left">
          <span class="footer-name">Talib Arkan Talib</span>
        </div>
        <div class="footer-right">
          <a href="mailto:talb.arkan2003@gmail.com" class="footer-icon" aria-label="Email">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
              <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
          </a>
          <a href="https://www.linkedin.com/in/talb-arkan-192a58254" target="_blank" rel="noreferrer" class="footer-icon" aria-label="LinkedIn">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
          </a>
        </div>
      </footer>
    </div>

    <script src="/js/theme.js" defer></script>
    <script src="/js/cursorEffects.js" type="module" defer></script>
    <script src="/js/certificates.js" type="module" defer></script>
  </body>
  </html>


